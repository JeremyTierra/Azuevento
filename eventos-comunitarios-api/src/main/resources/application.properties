spring.application.name=eventos-comunitarios-api

# Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-d6ukp314tr6s73d5va8g-a/community_events_pem5?sslmode=require
spring.datasource.username=azuevento_user
spring.datasource.password=nKZu1EWk8hwz5e9YPhyRRfa7AYyubNby
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=azuevento-production-secret-key-2026-muy-seguro
jwt.expiration=86400000

# CORS Configuration (allow frontend)
cors.allowed-origins=*

# Logging
logging.level.ec.edu.ucuenca.eventos=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Springdoc OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.default-produces-media-type=application/json
springdoc.default-consumes-media-type=application/json

# CRÍTICO: Evitar que Springdoc genere schemas de entidades JPA
springdoc.model-and-view-allowed=false
springdoc.override-with-generic-response=false
springdoc.packages-to-exclude=ec.edu.ucuenca.eventos.model

# Jackson Configuration (evitar referencias circulares)
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.properties.hibernate.enable_lazy_load_no_trans=true
